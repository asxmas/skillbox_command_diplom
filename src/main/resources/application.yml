spring:
  datasource:
    username: ${USERNAME}
    password: ${PASS}
    url: jdbc:postgresql://localhost:5432/skillbox_team13
  liquibase:
    enabled: true
    change-log: db/db.yml
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  token:
    secret: stream-team13
    expired: 120000