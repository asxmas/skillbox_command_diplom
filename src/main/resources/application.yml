spring:
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/skillbox_team13
  #  liquibase:
  #    enabled: true
  #    change-log: db/db.yml
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true